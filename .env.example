# WSD-Pro v2 — copy this file to `.env` and fill in the required values.

# Required — chatbot (qwen3:30b) key from https://ollama.com/settings/keys
OLLAMA_API_KEY=ollama_xxx

# Required — free OpenCode Zen API key (Big Pickle model).
# Get it from https://opencode.ai/auth (sign in with GitHub, no credit card).
OPENCODE_API_KEY=

# Optional — fixed IDE (code-server) password.
WSD_IDE_PASSWORD=admin123

# Optional — model name on Ollama Cloud.
WSD_CHAT_MODEL=qwen3:30b

# Optional — host port for the opencode web UI.
WSD_OPENCODE_PORT=4096
